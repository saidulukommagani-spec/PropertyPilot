FROM eclipse-temurin:21-jdk-alpine AS build

WORKDIR /workspace

COPY .mvn/ .mvn/
COPY mvnw pom.xml ./
RUN chmod +x mvnw && ./mvnw -B -ntp dependency:go-offline

COPY src/ src/
RUN ./mvnw -B -ntp package -DskipTests

FROM eclipse-temurin:21-jre-alpine

RUN addgroup -S propertypilot && adduser -S propertypilot -G propertypilot

WORKDIR /app
COPY --from=build /workspace/target/propertypilot-backend-*.jar app.jar

USER propertypilot
EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]
